/** @type {import('next').NextConfig} */
const nextConfig = {
  // Современный Next.js 15+ с App Router по умолчанию
  experimental: {
    // Можно добавить экспериментальные функции при необходимости
  },
};

module.exports = nextConfig;
